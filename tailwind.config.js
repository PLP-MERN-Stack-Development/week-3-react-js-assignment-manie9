module.exports = {
  darkMode: 'class', // Enable dark mode using the 'class' strategy
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Scan all files in the src folder
  theme: {
    extend: {}, // Extend the default theme if needed
  },
  plugins: [], // Add plugins if required
};